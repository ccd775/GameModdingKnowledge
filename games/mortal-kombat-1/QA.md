# 指南与工具自检记录

## 2026-09-15：公开仓库迁入

新增STATUS与Umbrella案例，明确T1000历史正常后复发、Conan武器候选离线通过但未安装。MK1接入仓库游戏目录、便携入口、公共init和单游戏/全游戏导出，工具只保留一份源码。历史本机证据名称保留说明，不上传原模型/游戏资源/私人报告。

实际执行18项MK1合成测试、3个CLI帮助检查及PowerShell合成部署（4项安全拒绝）；根13项组件测试、七份独立导出测试、公开文件链接/JSON/语法/hash检查通过。未启动或修改游戏。详细范围见[便携验证记录](../../portable-kits/VALIDATION.md)。

## 2026-09-14：Karin Chrome双角色v6b验收与身份门禁

- 新增 `cases/KARIN_CHROME_REIKO_LIUKANG.md`，并将Normal误作ART/CSM、末端指骨方向、刚性手掌有界回退、长大衣helper连续躯干场及UE重导入身份门禁接入README、端到端流程、材质、手部/上衣、打包、故障表、验证和Agent入口。
- 新增本机 `karin_chrome_reiko_liukang/acceptance-v6b.json` 与 `PLAYBOOK_HANDOFF.md`，重新读取两角色实际安装三件套、v6 FBX及已有FBX/包/部署报告hash，绑定用户原话“已经正常了，请将此次任务中学到的知识经验等沉淀总结进playbook”。历史pending报告保留，本轮未重新Cook、部署或启动游戏。
- 首次`Candidate-v6`因未重新导入UE Content而复用旧几何；Reiko容器hash与v4相同暴露问题。文档明确candidate路径变化不等于UAsset变化，几何版本须核对input FBX、导入报告、Content/Cooked身份和预期变化hash。真正重导入后的接受包为`Candidate-v6b`。
- 系统Python缺`markdown_it`时复用本机隔离QA依赖运行检查。初检发现4个既有`.pyc`，将这些明确缓存可恢复地移到本机工作区备份，没有删除源码或降低规则；重跑检查198个文件，文档链接、JSON、Python语法及公开边界通过。
- 本轮未修改共享算法或部署脚本，因此未把历史合成测试冒称重跑。E4只覆盖用户实际确认的材质亮度、手部和大衣下摆，不扩张为逐指动作、全部动画/LOD、独立衣物物理、断肢或联网认证。

## 2026-09-14：Karin Y v9验收与复发规则

- 新增 `UPPER_BODY_HANDS.md` 和 `cases/KARIN_Y_TRIO.md`；将指段方向、同源衣长、共享挂件锚点、Gear武器判定、删除覆盖的chunk/path/store合同接入流程、故障表、验证规范和项目模板。
- 本机v9验收脚本重新读取源Blend、三目标可编辑Blend/FBX、原生body、机器报告、九个安装/交付文件及九个v7备份hash，并绑定用户原话“可以了，请将此次任务中学到的知识经验等沉淀总结进playbook”。未重新Cook、启动或部署游戏；历史60姿势和包回读只作为已存在证据引用。
- 共享脚本合成回归实际运行：test_tools.py 3组、test_psk_index_plan.py 4组、test_retarget_math.py 6组、test_neck_fit.py 5组，全部通过。未改变这些算法，也未重新运行部署器或六游戏便携导出测试。
- 共享仓库检查193个文件，链接、JSON、Python语法及公开边界通过。复用已有隔离markdown-it依赖；初检发现3个历史Python缓存，精确移至本机工作区保留后重跑，没有降低规则。
- 本机交接入口36个相对链接、v9/v7构建快照语法、验收JSON身份检查通过。角色专属代码保持本机路径及源/目标限制，未冒充新增便携一键工具。
- E4仅表示用户目前查看的三角色效果接受，未扩大到全部动作、武器握持状态、LOD、所有终结技/断肢、物理碰撞或联网兼容。旧机器报告的runtime字段未改写；后续用户验收单独追加。

## 2026-09-13：头颈比例跨会话复发预防

- 新增 `NECK_PROPORTIONS.md`，覆盖可见颈长与骨距区别、肩宽后复查、头/长发整组平移、颈部C1过渡、冻结接受基线、原生动画枢轴偏差和增量打包。
- 接入入口、绑定规范、重定向检查、故障表与项目模板；不只留在Wasakura任务的聊天或本地日志中。
- `python -B scripts/test_neck_fit.py`：5组通过，覆盖刚性头/低处长发、非修改点及混合权重、过渡端点/导数、单位等价、非法输入与翻折参数拒绝。未将基础算子测试称为真实模型/动画验证，也未改写既有构建器去调用它。
- 仓库检查191个文件：链接、JSON、Python语法及公开边界通过。系统Python缺markdown-it时复用本机已有隔离QA依赖。初检发现两份既有pyc，精确移出到本机工作目录保留后重跑；没有改低检查规则。
- 本轮重新核对Wasakura三目标实际安装三件套hash和历史报告hash，新增局部反馈清单，保留用户原话“脖子好了很多”。历史43姿势/最终body回读未重跑，游戏/模型文件未改变，不进行重新Cook或部署。
- 颈部常量权重导数门槛不代表完整空间Jacobian安全；全动画/碰撞/LOD/断肢仍未认证。此轮没有重跑部署器合成测试或六游戏便携导出测试。

## 2026-09-13：UnendingFlame复发故障预防与赛克特尾项

- 新增 `test_retarget_math.py`：6组通过，覆盖轴向/横向分離、正反向骨轴、显式末端尺度、共享骨盆场的裤子高度/髋宽及翻折拒绝、C1过渡、可变页高图集坐标和非有限输入拒绝。
- `test_tools.py` 原有3组、`test_psk_index_plan.py` 原有4组均重跑通过。未修改或重跑部署合成测试，不将历史记录称为本轮测试。
- 仓库 `check_repository.py` 本轮检查188个文件，文档链接/JSON/Python语法/公开边界通过。系统Python缺markdown-it时，复用已有隔离QA依赖目录，不将依赖或pyc写入共享知识库。
- Sektor V7仅新增无SK_前缀的ClothMesh包，原有25个package完整保留；8200三角形的完整PSK证据、源/目标hash、native store与最终全chunk回读通过，已安装并验证其它30个活动自定义文件未变。V7隐藏效果仍待实机反馈。
- V6身体/体型/裤子/鞋部的接受记录来自用户“其他的看起来都可以了”，不扩大为所有动画/LOD/断肢；Kenshi/Cyrax未因赛克特补丁再次部署。
- 原创基础算子仅合成验证，未声称所有既有工程已迁移使用它们。新构建器需实际采集报告并验证自己的输入/输出。

本次沉淀完成时实际运行：

- `scripts/test_tools.py`：3组测试通过，覆盖raw差异allowlist、受hash约束的索引补丁、越界/重叠/hash错误/覆盖拒绝、RGBA图集方向和alpha。
- `scripts/test_deploy.ps1`：合成目录中预检不写盘、完整三件套备份/安装通过；4项hash/路径/prefix拒绝通过。测试中仅mock进程查询，不触碰真实游戏。
- 共享仓库 `tests/test_portable_tools.py`：12项通过。
- 共享仓库 `tests/test_export_portability.py`：原有六游戏detached export测试通过。**没有声称MK1已加入该导出器。**
- 共享仓库 `tools/check_repository.py`：本次新增文档/JSON/Python语法/链接和公开文件边界通过。

部署的断电/磁盘故障与文件锁恢复并未穷举，脚本保留备份供人工恢复。游戏运行验收来自Nyako→Sindel案例的用户反馈，而非上述合成测试。

当前通用工具为首次版本；新的角色工程仍须单独做游戏资源、源模型和运行校验。修改这些脚本之后重新运行测试，不覆盖旧记录来制造通过状态。

## 2026-09-12：三角色经验与 PSK 工具增补

- `test_psk_index_plan.py`：4 组通过；完整流/winding/重复区间、32位、前96项相同但尾部不同的拒绝、损坏/越界/未匹配LOD、与索引补丁器集成。
- `test_tools.py`：原有3组重新通过。本次未修改部署器，未将旧部署合成测试冒充为本轮重跑。
- 本地实际 Face001/Face002/两个 Mask 资源：共享工具生成4份计划、5段索引，与已接受 v10 案例计划的源hash、offset、width、count、vertex_count、buffer hash 完全一致。该只读回归不修改已接受包，也不补足未知LOD证据。
- 三角色源 Blend、可编辑 Blend/FBX、Output 与游戏安装三件套重新核对；本地验收manifest记录实际hash与用户原话，不含账号/密码。
- `tools/check_repository.py`：链接、JSON/Python语法及公开边界检查通过。初次检查发现先前构建导入留下的 `.pyc`；将该精确缓存文件移到本地工作区保留后重跑通过，未修改检查器的公开边界规则。

便携指南仅新增文档和原创小工具，未纳入游戏/模型payload、密钥、配置全量dump；仍未接入六游戏 `export_kit --all` 发布流水线。用户接受的是三角色当前可见效果，不等于脚本或游戏的全场景保证。

## 2026-09-13：尾部经验沉淀与验收冻结

- 新增 `APPENDAGES.md`，区分静止几何适配与动画骨骼归属，记录连通块范围、根部中心锚定、源形相似变换、非修改集合和跨角色增量迁移边界。
- 本轮只读验收工具核对4个目标的源SHA、Blend/FBX与尾部修复报告SHA、最终包与实际安装三件套SHA；新写尾部验收清单成功，未覆盖旧v9/v10记录。
- 尾部构建时已有的25姿势/目标、源形局部几何、完整包回读与非body chunk不变证据被引用并锁定；本轮没有重新Cook/部署，也没有将历史检查冒称重新运行。
- 本地10份入口/工程文档链接、6份三角色配置状态与新验收脚本语法检查通过。
- 共享仓库 `tools/check_repository.py` 文档/JSON/语法/公开边界通过。初次发现2个生成的 `.pyc`，精确移至本机工作报告目录保留后重跑；未降低公开文件检查规则。

Kitana tail-v2与三个角色tail-v11的E4来自用户分别确认“kitana的正常了”和“看起来可以了”，仅限实际看到的效果，不证明独立尾骨/碰撞物理、所有LOD、全部动作或断肢兼容。
## 2026-09-14：Riptide最终经验与验收冻结

- 新增 Riptide 三角色案例和原生材质补丁指南；更新材质、绑定、打包、故障定位、端到端流程、脚本索引及 Agent 入口。
- 本次重新核对源模型、最终 Ermac v7 / Noob v6 / Ashrah v5 的九个候选及实际安装文件哈希，全部相符；独立接受清单锁定已有报告、脚本和 v5 可编辑几何。没有重新 Cook/部署，也没有把历史离线检查冒称本次重新运行。
- 原生 MI 补丁脚本保留在本机工程并注明限制，未作为通用编辑器分发。共享库只新增原创说明，不包含模型/游戏 payload、密钥或配置全量 dump。
- 共享仓库 `tools/check_repository.py` 检查195个文件，文档链接、JSON、Python语法与公开边界通过。没有修改共享数学或部署脚本，因此本轮未重跑其合成测试或六游戏便携导出。
- 用户接受当前可见结果；全战斗动作、所有皮肤/装备/LOD、断肢和联网仍未认证。历史 pending 保留，当前项目入口指向后续接受记录。
